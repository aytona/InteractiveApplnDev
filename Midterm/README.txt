BankAccount
* Assuming that every type of BankAccount has some sort of interest rate, I have included CalculateInterestRate in to be overriden
* Balance can only be changed through account creation, withdrawal, or deposit

Savings Account
* Assumes that all savings accounts have an interest rate of 10%

Client
* Assumes that if no banking info is inputted, a basic one is added

IDs
* Assuming if IDs aren't specified, then they are all set to 0
* Assuming that client IDs and bank account IDs are separate things